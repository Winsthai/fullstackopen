import { styled } from "@mui/system";
import { Box } from "@mui/material";

export const EntryContainer = styled(Box)({
  padding: 8,
  border: "2px solid",
  borderRadius: "4px",
  marginBottom: "1em",
});
